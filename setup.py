from setuptools import setup

setup(
   name='ckaccess',
   version='1.0',
   description='A Web service to check access to other web services',
   author='Adam Young',
   author_email='ayoung@redhat.com',
   packages=['ckaccess'],
   install_requires=[], 
)
